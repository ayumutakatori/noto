<!DOCTYPE html>
<html>
    <head>
        {% include "head.liquid" %}
    </head>
    <body>
        {% include "header.liquid" %}

        <div class="content">
            <h2 class="title">{{ page.title }}</h2>
            <div class="description">
                　Date：{{ page.published_date }}<br />
                　Categories：{% for category in page.categories %} {{ category }} {% endfor %}<br />
            </div>
            {{ page.content }}
            <a href="https://twitter.com/share?ref_src=twsrc%5Etfw" class="twitter-share-button" data-show-count="false" data-text="{{ page.title }} - NOTO (@katsumata_ryo)">Tweet</a><script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
        </div>
        <div class="footer">
            {% include "footer.liquid" %}
        </div>
    </body>
</html>
