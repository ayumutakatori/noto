<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ page.title }}</title>
    {% comment %}<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">{% endcomment %}
    <link rel="stylesheet" href="/public/css/base.css">

</head>
<body>

<h1>PAPYRUS</h1>

<div class="content">
    <h2 class="title">{{ page.title }}</h2>
    <div class="description">
        　Date：{{ page.published_date }}<br />
        　Categories：{% for category in page.categories %} {{ category }} {% endfor %}<br />
        　Tags：{% for tag in page.tags %} {{ tag }},  {% endfor %}<br />
    </div>
    {{ page.content }}
</div>
<div class="footer">
    <a href="/">TOP</a>
</div>
</body>
</html>
